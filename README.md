# Purohitham - MVP (Vite + React + Tailwind)

## Quick start
1. Extract the zip.
2. Install dependencies: `npm install`
3. Run local dev server: `npm run dev`
4. To build: `npm run build`
5. Deploy by connecting the repository to Vercel (one-click).

The booking form is embedded as a Google Form in the Booking section.